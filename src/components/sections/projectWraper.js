import React from "react";
import styled from "styled-components";

const Wrapper = styled.section`
  height: 500px;
`;

const ProjectWrapper = () => {
  return <Wrapper>ProjectWrapper</Wrapper>;
};

export default ProjectWrapper;
